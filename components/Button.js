import { TouchableOpacity, View, Text } from "react-native";
import { COLORS, SIZES } from "../constants";

export const LockButton = () => {
  return <div>lockButton</div>;
};

export const UnlockButton = () => {
  return <div>unlockButton</div>;
};
